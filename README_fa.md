# fptn-manager

🌐 زبان‌ها: [English](README.md) | [فارسی](README_fa.md)

پروژه `fptn-manager` یک ابزار خط فرمان ساده و منویی است برای نصب و مدیریت **FPTN VPN SERVER**

هدف این پروژه این است که راه‌اندازی و استفاده از FPTN VPN روی یک سرور لینوکسی
ساده و بدون دردسر شود؛ بدون اینکه لازم باشد درگیر Docker، فایل‌های compose
یا تنظیمات خاص و پیچیده شوید.

---

## ⚙️ پروژه FPTN چگونه کار می‌کند

این پروژه یک VPN در لایه‌ی شبکه (Layer-3) است که ترافیک خام IP را از طریق ارتباطی عبور می‌دهد که از بیرون شبیه ترافیک عادی HTTPS به نظر می‌رسد. در سمت کلاینت، یک رابط TUN بسته‌های IPv4 و IPv6 را دریافت می‌کند و آن‌ها را از طریق یک تونل امن به سرور ارسال می‌کند. سرور پس از انجام NAT، ترافیک را به اینترنت هدایت می‌کند. همچنین امکان Split Tunneling وجود دارد تا فقط ترافیک مربوط به دامنه‌ها یا شبکه‌های مشخص از داخل VPN عبور کند.

برخلاف VPNهای متداول، FPTN از پروتکل‌های استانداردی مانند OpenVPN یا WireGuard استفاده نمی‌کند. بسته‌های IP ابتدا با استفاده از Protocol Buffers سریالایز می‌شوند و سپس از طریق یک کانال سفارشی مبتنی بر TLS (بر پایه‌ی BoringSSL) منتقل می‌گردند. در سطح پروتکل، داده‌ها به‌صورت تصادفی پد می‌شوند تا الگوی ترافیک قابل تشخیص نباشد و تحلیل آن سخت‌تر شود.

تمرکز اصلی FPTN بر مقاومت در برابر فیلترینگ و Deep Packet Inspection است. اتصال VPN روی پورت 443 برقرار می‌شود و رفتارش شبیه یک ارتباط معمولی HTTPS است. کلاینت‌های معتبر در همان مرحله‌ی TLS و با استفاده از یک `session_id` تغییر یافته شناسایی می‌شوند، بدون اینکه فاز مشخصی برای مذاکره‌ی VPN قابل مشاهده باشد. کلاینت از روش‌هایی مانند جعل SNI، مبهم‌سازی TLS و «حالت Reality» پشتیبانی می‌کند؛ حالتی که در آن اتصال در ابتدا دقیقاً مثل یک نشست واقعی HTTPS رفتار می‌کند و سپس به‌صورت نامحسوس به تونل VPN تبدیل می‌شود. اگر اتصالی با الگوی مورد انتظار مطابقت نداشته باشد، سرور ترافیک را به‌صورت شفاف به دامنه‌ی درخواست‌شده پروکسی می‌کند، به‌طوری که از بیرون کاملاً شبیه یک وب‌سایت عادی HTTPS دیده می‌شود.

احراز هویت کاربران پس از برقراری کانال امن و با نام کاربری و رمز عبور انجام می‌شود. سرور می‌تواند محدودیت پهنای باند برای هر کاربر اعمال کند، ترافیک ناخواسته مانند BitTorrent را شناسایی و مسدود نماید و از طریق یک REST API مبتنی بر JWT مدیریت شود. همچنین امکان خروجی گرفتن از متریک‌ها برای Prometheus، استفاده از Grafana، و راه‌اندازی معماری کلاستر (master/slave) وجود دارد. برای ساده‌سازی تنظیم کلاینت‌ها، FPTN از یک قالب توکن استفاده می‌کند که اطلاعات اتصال را به‌صورت یکپارچه در اختیار کلاینت قرار می‌دهد.

برای جزئیات بیشتر درباره‌ی پیاده‌سازی، پروتکل و کلاینت‌ها، به مخزن اصلی پروژه مراجعه کنید:
https://github.com/batchar2/fptn

---

## 📦 پیش‌نیازها

- یک VPS یا سرور لینوکسی
- دسترسی root (یا `sudo`)
- توزیع‌های پشتیبانی‌ شده:
  - Ubuntu / Debian
  - Rocky / Alma / RHEL
- سایر توزیع‌های مبتنی بر systemd

نکته: اگر Docker و Docker Compose نسخه ۲ روی سیستم نصب نباشند، به‌ صورت خودکار نصب خواهند شد.

---

## 🚀 نصب

روش پیشنهادی و رسمی نصب:

```bash
curl -fsSL https://raw.githubusercontent.com/FarazFe/fptn-manager/main/fptn-manager.sh \
  -o /tmp/fptn-manager && sudo bash /tmp/fptn-manager
```

بعد از اتمام نصب، دستور `fptn-manager` به‌صورت سراسری در سیستم در دسترس خواهد بود.

---

## 🧭 نحوه استفاده

برای اجرای پنل مدیریت، دستور زیر را اجرا کنید:

```bash
sudo fptn-manager
```

یک منوی تعاملی مشابه زیر نمایش داده می‌شود:

<img width="1347" height="506" alt="Screenshot from 2026-02-08 12-10-16" src="https://github.com/user-attachments/assets/505abf25-8f55-43fc-abe3-cf009a224a36" />


---

## 🟢 نصب ساده (Easy Install – پیشنهاد شده)

اگر فقط می‌خواهید سریع VPN را راه‌اندازی کنید، این بهترین گزینه است.

در این حالت:

- از تنظیمات پیش‌فرض و معقول استفاده می‌شود
- IP عمومی سرور به‌صورت خودکار تشخیص داده می‌شود
- همه‌چیز داخل مسیر `/opt/fptn` نصب می‌شود
- سرویس VPN بلافاصله اجرا می‌شود
- **در هر بار اجرا، یک کاربر جدید VPN ساخته می‌شود**
- از شما خواسته می‌شود یک بار رمز عبور را وارد کنید
- یک توکن آماده‌ی استفاده برای کلاینت نمایش داده می‌شود

به‌دلیل ایجاد کاربر جدید در هر اجرا، می‌توانید Easy install را چند بار اجرا کنید
بدون اینکه کاربران یا توکن‌های قبلی دچار مشکل شوند.

---

## 📱 کلاینت‌ها (Android / Linux / Windows / macOS)

پس از نصب و راه‌اندازی سرور، ابزار مدیریت یک **TOKEN** با پیشوند `fptn:` نمایش می‌دهد.  
کل توکن را به طور کامل کپی کرده و در برنامهٔ کلاینت وارد کنید تا اتصال برقرار شود.

### ✅ اندروید
- گوگل پلی: https://play.google.com/store/apps/details?id=org.fptn.vpn

### 🖥 ویندوز / 🐧 لینوکس / 🍎 macOS
کلاینت‌های رسمی دسکتاپ را از صفحهٔ انتشارهای پروژهٔ اصلی دانلود کنید:
- انتشارهای پروژه اصلی: https://github.com/batchar2/fptn/releases

> نکته: نسخه‌ی مناسب سیستم‌عامل و معماری پردازنده‌ خود را انتخاب کنید (x86_64 یا arm64).

---

## 👤 کاربران، رمز عبور و توکن‌ها

چند نکته مهم:

- رمز عبور کاربران همیشه **داخل کانتینر** و با ابزار `fptn-passwd` تنظیم می‌شود
- توکن فقط بعد از وارد کردن دوباره‌ی همان رمز عبور ساخته می‌شود
- این کار باعث می‌شود رمز ذخیره‌شده روی سرور و رمز داخل توکن همیشه یکی باشند

### ریست کردن رمز عبور یک کاربر

از گزینه‌ی زیر در منو استفاده کنید:

```
11) Generate token (existing user / reset password)
```

اگر گزینه‌ی ریست رمز را انتخاب کنید:

1. کاربر به‌صورت تعاملی حذف می‌شود (نیاز به تأیید دارد)
2. همان کاربر دوباره ساخته می‌شود
3. از شما خواسته می‌شود رمز جدید وارد کنید
4. یک توکن جدید تولید می‌شود

این امن‌ترین راه برای رفع خطای «wrong password» در کلاینت است.

---

## 📁 ساختار فایل‌ها

ساختار پیش‌فرض نصب:

```
/opt/fptn/
├── docker-compose.yml
├── .env
└── fptn-server-data/
    ├── server.key
    └── server.crt
```

فایل تنظیمات مدیر:

```
/etc/fptn/manager.conf
```

---

## 🔄 مدیریت سرویس

اجرای سرویس:
```bash
sudo fptn-manager
# گزینه: Start service
```

توقف سرویس:
```bash
sudo fptn-manager
# گزینه: Stop service
```

مشاهده وضعیت:
```bash
sudo fptn-manager
# گزینه: Show status
```

مشاهده لاگ‌ها:
```bash
sudo fptn-manager
# گزینه: View logs
```

به‌روزرسانی ایمیج سرور:
```bash
sudo fptn-manager
# گزینه: Update (pull latest image)
```

---

## 🔐 گواهی SSL

- در صورت نبود، گواهی SSL به‌صورت خودکار ساخته می‌شود
- فایل‌ها به‌صورت دائمی در مسیر `/opt/fptn/fptn-server-data` ذخیره می‌شوند
- می‌توانید fingerprint گواهی را از منو مشاهده کنید (در صورت نیاز کلاینت)

---

## 🛠 عیب‌یابی

بررسی وضعیت Docker:
```bash
systemctl status docker
```

بررسی وضعیت کانتینرها:
```bash
cd /opt/fptn
docker compose ps
```

مشاهده مستقیم لاگ‌های سرور:
```bash
cd /opt/fptn
docker compose logs -f fptn-server
```

اگر کلاینت خطای **«wrong password»** نمایش داد:
- گزینه **11** را اجرا کنید
- ریست رمز عبور را انتخاب کنید
- توکن جدید بسازید و دوباره امتحان کنید

---

## 📜 لایسنس

MIT License  
استفاده، تغییر و بازنشر این پروژه آزاد است.

---

## ❤️ قدردانی

- پروژه FPTN VPN Server: https://github.com/batchar2/fptn
